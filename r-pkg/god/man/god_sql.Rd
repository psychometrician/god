% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{god_sql}
\alias{god_sql}
\title{The query a pipeline becomes}
\usage{
god_sql(pipeline, columns)
}
\arguments{
\item{pipeline}{The pipeline, as text.}

\item{columns}{The table's columns, as `name:type` separated by commas.}
}
\value{
The query, as text.
}
\description{
The query a pipeline becomes
}
\examples{
sales <- data.frame(
  region  = c("West", "East", "West"),
  revenue = c(100, 120, 150)
)
cat(god_sql("sales then take 1", "region:text,revenue:number"))
#> WITH step0 AS (SELECT * FROM "sales"),
#>      step1 AS (SELECT * FROM step0 LIMIT 1)
#> SELECT * FROM step1
}
