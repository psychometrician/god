% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{show_as}
\alias{show_as}
\title{The same pipeline, written in a language you already know}
\usage{
show_as(pipeline, as = "dplyr", ...)
}
\arguments{
\item{pipeline}{The pipeline, as text.}

\item{as}{Which language. `"sql"`, `"spark"`, `"dplyr"`, `"pandas"`,
`"polars"`, `"pyspark"`, or `"god"` itself. An unknown name is refused and
the message lists the real ones, so this list going stale costs nothing.}

\item{...}{Tables, named, if the one at the head is not in scope.}
}
\value{
The text, invisibly, after printing it.
}
\description{
A small vocabulary covers most of what people do and never all of it, so the
question is not whether you reach its edge but what happens when you do.
}
