% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{add}
\alias{add}
\title{Add or replace a column}
\usage{
add(.data, ..., by)
}
\arguments{
\item{.data}{A table, or a pipeline.}

\item{...}{Named columns: `margin = revenue - cost`.}

\item{by}{Columns to group by. An aggregate is then broadcast back over each
group rather than collapsing it: `add(share = revenue / total(revenue), by = product)`.}
}
\value{
A pipeline.
}
\description{
Add or replace a column
}
\examples{
sales <- data.frame(
  region  = c("West", "East", "West"),
  revenue = c(100, 120, 150)
)
sales |> add(doubled = revenue * 2)
}
