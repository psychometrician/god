% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verbs.R
\name{summarize}
\alias{summarize}
\title{Collapse to one row per group}
\usage{
summarize(.data, ..., by)
}
\arguments{
\item{.data}{A table, or a pipeline.}

\item{...}{Named columns, each one an aggregation:
`margin = total(margin)`.}

\item{by}{Columns to group by. Without it the whole table is one group.}
}
\value{
A pipeline.
}
\description{
Collapse to one row per group
}
\examples{
sales <- data.frame(
  region  = c("West", "East", "West"),
  revenue = c(100, 120, 150)
)
sales |> summarize(sold = total(revenue), by = region)
}
