% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run}
\alias{run}
\title{Run a pipeline}
\usage{
run(pipeline, ...)
}
\arguments{
\item{pipeline}{The pipeline, as text.}

\item{...}{Tables, named. Usually unnecessary: the table named at the head of
the pipeline is looked up where you are calling from, the way
`duckdb.sql("SELECT * FROM df")` finds `df` in Python.}
}
\value{
A data frame.
}
\description{
Run a pipeline
}
\examples{
sales <- data.frame(
  region  = c("West", "East", "West"),
  revenue = c(100, 120, 150)
)
run('
  sales
    then keep where [region] is "West"
', sales = sales)
}
